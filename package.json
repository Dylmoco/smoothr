{
  "name": "smoothr-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "smoothr",
    "storefronts"
  ],
  "scripts": {
    "test": "npm --workspace storefronts test && npm run test:supabase",
    "test:supabase": "npm --workspace storefronts exec vitest run ../supabase/functions/**/*.test.ts"
  }
}
